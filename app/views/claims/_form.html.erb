<%= simple_form_for @claim do |f| %>
  <%= f.input :user_id,
            value_method: :id,
            collection: @users,
            include_blank: false,
            input_html: {class: 'chosen-select', multiple:true, id: 'patient_id'},
            prompt: 'Select users'
  %><br><br>

  <% end %>
