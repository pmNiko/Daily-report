

  - if claim.errors.any?
    %div#error_explanation
      %h2 = pluralize(claim.errors.count, "error")  prohibited this claim from being saved:

    %ul
      - claim.errors.full_messages.each do |message|
      %li
        = message

        %div.field
          = form.label :ticket
          = form.number_field :ticket, id: :claim_ticket

        %div.field
          = form.label :client
          = form.text_field :client, id: :claim_client

        %div.actions
          = form.submit

          - f.select :user,
            User.all.map { |u| [u.email, u.id] },
                         { include_blank: true },
                         { class: 'chosen-select', multiple: true}

 = f.select :user_id, User.all.map{|user| [user.email, user.id]}
                                  { include_blank: true },
                                  { class: 'chosen-select', multiple: true}
<%= f.association :law_guardians, input_html: {'data-placeholder' => 'Select L Guardian'}, label: 'L Guardians', :label_method => lambda { |lg| "#{lg.cl_name}, #{lg.cf_name}" } %>
