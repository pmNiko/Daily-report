<nav class="navbar fixed-top navbar-expand-sm navbar-dark bg-dark">
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-content"
 aria-controls="nav-content" aria-expanded="false" aria-label="Toggle navigation">
<span class="navbar-toggler-icon"></span>
</button>

<!-- Brand -->
<a class="navbar-brand" href="#">Logo</a>

<!-- Links -->
<div class="collapse navbar-collapse" id="nav-content">
  <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <% if user_signed_in? %>
          <%= link_to 'Claims', claims_path, class: 'navbar-brand' %>
          <a href="#" class="navbar-brand">
            <% if current_user.profile.first_name.present? %>
              <%= current_user.profile.first_name %>
            <% else %>
              <%= current_user.email %>
            <% end %>
          </a>
          <%= link_to 'Profile', edit_profile_path, :remote => true, class: 'navbar-brand' %>
          <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete', class: 'navbar-brand'  %>
          <%= link_to 'New Team', new_team_path, :remote => true, class: 'navbar-brand' %>
          <%= link_to 'Teams', teams_path, class: 'navbar-brand' %>
          <%= link_to 'Calendar', events_path, class: 'navbar-brand' %>
        <% end %>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <%= render 'layouts/navigation_links' %>
        </ul>
      </div>
</nav>
