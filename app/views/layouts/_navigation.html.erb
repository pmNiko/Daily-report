<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to 'Claims', claims_path, class: 'navbar-brand' %>
        <p class="navbar-text">
          <% if current_user.profile.first_name.present? %>
            <%= current_user.profile.first_name %>
          <% else %>
            <%= current_user.email %>
          <% end %>
        </p>
        <%= link_to 'Profile', edit_profile_path, class: 'navbar-brand'  %>
        <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete', class: 'navbar-brand'  %>
        <%= link_to 'New Team', new_team_path, class: 'navbar-brand' %>
        <%= link_to 'Teams', teams_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
